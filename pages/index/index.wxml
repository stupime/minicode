<!--构建页面-->
<view class = "swiper-tab">
  <view 
    class = "swiper-tab-list {{currentTab == 0 ? 'on' : ''}}" 
    data-current = "0"
    bindtap = "swichNav"> 课表 </view>
  <view 
    class = "swiper-tab-list {{currentTab == 1 ? 'on' : ''}}" 
    data-current = "1"
    bindtap = "swichNav"> 活动 </view>
  <view 
    class = "swiper-tab-list {{currentTab == 2 ? 'on' : ''}}" 
    data-current = "2"
    bindtap = "swichNav"> 课程评价 </view>
</view>

<text>{{ currentTab }}</text>

<view wx:if = "{{currentTab == 0}}"
>hahaha</view>

<scroll-view
  class = "test"
  scroll-y
  style = "height: 1000px">
</scroll-view>
